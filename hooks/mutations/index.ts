/**
 * Export tất cả mutation hooks
 * Import từ đây để sử dụng: import { useXXXMutation } from '@/hooks/mutations';
 */

// Thêm các mutation hooks ở đây khi tạo mới
// export { useCreateUserMutation, type CreateUserRequest, type CreateUserResponse } from './useCreateUserMutation';
