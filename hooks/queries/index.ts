/**
 * Export tất cả query hooks
 * Import từ đây để sử dụng: import { useHomePage } from '@/hooks/queries';
 */

export { useHomePage, type HomePageData, type UseHomePageOptions } from './useHomePage';
